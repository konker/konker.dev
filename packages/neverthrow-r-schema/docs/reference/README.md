**@konker.dev/neverthrow-r-schema**

***

# @konker.dev/neverthrow-r-schema

Standard Schema adapters for `neverthrow` and `@konker.dev/neverthrow-r`.

Use this package when a Standard Schema should be parsed inside a
`Result` / `ResultAsync` / `ResultR` / `ResultAsyncR` chain without throwing.

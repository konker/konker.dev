# neverthrow-r

An extension for [neverthrow](https://github.com/supermacro/neverthrow) which adds a third requirements/dependencies channel (`R`) to `Result` and `ResultAsync`.

See `docs/design/v1-reader-function.md` for the full v1 specification.

---
import type { CollectionEntry } from 'astro:content';
import { itemIconLookup } from '../../lib/collections/helpers';
import type { MoonlightCollectionName } from '../../lib/moonlight/config';
import { ITEM_ICON_SIZE } from '../../scripts/consts';
import ArticleMetadata from '../site/ArticleMetadata.astro';

interface Props {
  readonly item: CollectionEntry<MoonlightCollectionName>;
  readonly itemIconName?: string | undefined;
}

const { item, itemIconName } = Astro.props;
const ItemIcon = itemIconLookup(itemIconName);
---

<article
  class={[
    'project-post',
    'index-item',
    'mb-6',
    'pt-8',
    'pb-1',
    'px-8',
    'min-h-32',
    'border-b-4',
    'border-gray-300',
    'dark:border-gray-500',
    itemIconName,
  ].join(' ')}
>
  <div class="wrapper pb-2">
    <h2 class="mb-5 font-headings font-bold text-xl">
      <span class="separator float-left inline-block mt-1 md:mt-0 md:mb-[-0.30rem]"
        ><ItemIcon size={ITEM_ICON_SIZE} /></span
      >
      <a href={item.slug} class="block pl-[36px]">{item.data.title}</a>
    </h2>
    <p class="abstract mb-6 font-serif">
      {item.data.description}
      <br>
      <span class="block text-right"><a href={item.slug} class="inline-block mt-3 mr-3 px-2 font-sans rounded-md bg-kdd-nav">Read more...</a></span>
    </p>
    <ArticleMetadata tags={item.data.tags} />
  </div>
</article>

<style>
  article.project-post {
    /* @apply 'bg-gray-200'; */
    background-color: rgb(229 231 235);
  }
  article.project-post h2 a {
    text-decoration: none;
    color: var(--kdd-color-heading);
  }

  article.project-post.blog,
  article.project-post.til {
    @apply bg-kdd-blog;
  }

  article.project-post.project,
  article.project-post.projects {
    @apply bg-kdd-project;
  }
</style>

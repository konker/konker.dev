---
import type { CollectionEntry } from 'astro:content';
import { itemIconLookup } from '../../lib/collections/helpers';
import type { MoonlightCollectionName } from '../../lib/moonlight/config';
import { ITEM_ICON_SIZE } from '../../scripts/consts';
import ArticleMetadata from '../site/ArticleMetadata.astro';

interface Props {
  readonly item: CollectionEntry<MoonlightCollectionName>;
  readonly itemIconName?: string | undefined;
}

const { item, itemIconName } = Astro.props;
const ItemIcon = itemIconLookup(itemIconName);
---

<article
  class={[
    'blog-post',
    'index-item',
    'mb-6',
    'pt-6',
    'pb-1',
    'px-8',
    'min-h-32',
    'border-b-4',
    'border-gray-300',
    'dark:border-gray-500',
    itemIconName,
  ].join(' ')}
>
  <div class="wrapper pb-2">
    <h2 class="mt-0 mb-5 py-2 pl-1 pr-6 font-sans text-lg rounded-md bg-kdd-nav">
      <span class="separator float-left inline-block mt-1 md:mt-0 md:mb-[-0.30rem] px-1"
        ><ItemIcon size={ITEM_ICON_SIZE} /></span
      >
      <a href={item.slug} class="block pl-[36px]">{item.data.title}</a>
    </h2>
    <p class="abstract mb-6 font-serif">{item.data.description}</p>
    <ArticleMetadata tags={item.data.tags} />
  </div>
</article>

<style>
  article.blog-post {
    /* @apply 'bg-gray-200'; */
    background-color: rgb(229 231 235);
  }

  article.blog-post.blog,
  article.blog-post.til {
    @apply bg-kdd-blog;
  }

  article.blog-post.project,
  article.blog-post.projects {
    @apply bg-kdd-project;
  }
</style>
